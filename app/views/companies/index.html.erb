<h1>Check out the premier companies that recruit at Duke...</h1>

<article>
<ul>
	<% @companies.each do |company| %>
		<%= image_tag company.image, :style => "width:100px;height:100px;" %>
		<h4><%= link_to company.name, company_path(company) %></h4>	
	<% end %>
</ul>
</article>
